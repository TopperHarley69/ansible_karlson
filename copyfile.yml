- name: copy file
  hosts: deploys
  gather_facts: yes
  become: yes

  tasks:
    - name: copy file
      copy:
       src: ~/.ssh/ansible_karlson/auditsettings.ps1
       dest: c:\temp\
      register: script_return
    - debug:
        msg: "{{ script_return }}"
  
